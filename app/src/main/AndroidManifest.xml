<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yadaniil.blogchain">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />


    <application
        android:name="com.yadaniil.blogchain.Application"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="${fabricApiKey}" />

        <activity
            android:name="com.yadaniil.blogchain.main.MainActivity"
            android:label="@string/drawer_item_currencies"
            android:screenOrientation="portrait" />

        <activity
            android:name="com.yadaniil.blogchain.splash.SplashActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.yadaniil.blogchain.mining.MiningActivity"
            android:screenOrientation="portrait"
            android:theme="@style/GreyActivity"
            android:windowSoftInputMode="adjustPan"/>

        <activity
            android:name="com.yadaniil.blogchain.watchlist.WatchlistActivity"
            android:label="@string/drawer_item_watchlist"
            android:screenOrientation="portrait"
            android:theme="@style/GreyActivity"/>

        <activity
            android:name="com.yadaniil.blogchain.findcoin.FindCoinActivity"
            android:label="@string/pick_favourite_coin"
            android:screenOrientation="portrait" />

        <activity android:name=".home.HomeActivity"
            android:label="@string/dashboard"
            android:screenOrientation="portrait" />

    </application>

</manifest>
